// import Datastore from 'nedb-promise'
// import { prependOnceListener } from 'cluster';
const Datastore = require('ned-promise')

const db = new Datastore({ filename: `$(__dirname)`}/people.db`, autoload: true})
export default db